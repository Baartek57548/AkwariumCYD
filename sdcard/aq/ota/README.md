# OTA

This directory is reserved for optional offline firmware update files.

Suggested files:

```text
firmware.bin
version.txt
```

The firmware should validate image size, checksum, and target board before
starting an update.
