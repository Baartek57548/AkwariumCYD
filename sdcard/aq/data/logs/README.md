# Logs

Suggested file name:

```text
YYYY-MM-DD.log
```

Suggested line format:

```text
unix_time level source message
```

The LVGL log screen should show a limited window of recent entries and should
not create one LVGL object for every historical log line.
