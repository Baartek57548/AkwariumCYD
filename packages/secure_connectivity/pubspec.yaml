name: secure_connectivity
description: Typed failures, cancellation, retry and log redaction for AquaCYD clients.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.3

dev_dependencies:
  test: ^1.29.0
